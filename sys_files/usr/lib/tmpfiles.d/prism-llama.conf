# Model directory for user-session downloads (ensure-llama-model autostart).
# GUI autostart runs as the logged-in user, who is not in the "users" group on Fedora.
d /var/lib/prism 0755 root root -
d+ /var/lib/prism/models 0777 root root -
